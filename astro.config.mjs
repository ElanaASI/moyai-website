// @ts-check
import { defineConfig } from 'astro/config';

// Hosted on GitHub Pages at https://<username>.github.io/moyai-website
// `site` is filled in once the GitHub account is known; `base` matches the repo name.
export default defineConfig({
  site: 'https://USERNAME.github.io',
  base: '/moyai-website',
});
